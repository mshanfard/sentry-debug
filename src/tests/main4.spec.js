import React from 'react';

jest.mock('../clients/logger');
import loggerMock from '../clients/logger';

import { mount }  from 'enzyme'
import App from '../main'

const error = 'logging to demo slow tests due to sentry'
describe('demo', () => {
  beforeEach(() => {
      mount(<App/>);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });
  
  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

  it('slows down tests', () => {
    expect(loggerMock.exception).toHaveBeenCalledWith(error);
  });

});